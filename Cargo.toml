[package]
name = "discord_LLM_news"
version = "0.1.0"
edition = "2024"

[dependencies]
# rss feed用クレート
rss = "2.0" 

# HTTPリクエストを簡単に送受信
reqwest = { version = "0.12", features = ["blocking"]}

# HTML解析用
scraper = "0.18"


# htmlをmdに変換する
html2md = "0.2.15"

